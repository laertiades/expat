<% provide(:title, 'All consultants') %>
<h1>All Consultants</h1>

<div class="row">
  <div class="span6 offset4">
    <ul class="consultants">
      <% @consultants.each do |con| %>
	<li><%= link_to con.name, edit_consultant_path(con) %></li>
      <% end %>
    </ul>
  </div>
</div>
